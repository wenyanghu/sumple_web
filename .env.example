PORT=3000

# Cloud: set to a PostgreSQL connection string (Neon, Supabase, Render, Railway, etc.)
# DATABASE_URL=postgresql://user:pass@host:5432/dbname

# SSL for managed Postgres (default on in production)
# DATABASE_SSL=true
